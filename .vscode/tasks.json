{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Launch Game",
            "type": "shell",
            "command": "${workspaceFolder}/Game.exe",
            "group": {
                "kind": "test",
                "isDefault": true
            }
        }
    ]
}